using System.Windows.Forms;

namespace Scribe
{
    /// <summary>
    /// A modal dialogue that enables the user to add a new <see cref="ParquetClassLibrary.ModelTag"/> to a collection of model tags.
    /// </summary>
    partial class AddTagBox : Form
    {
        /// <summary>
        /// Initialize a new <see cref="AddTagBox"/>.
        /// </summary>
        public AddTagBox()
        {
            InitializeComponent();
        }
    }
}
